{% extends 'apps/base.html.twig' %}

{% block title %}Modiier l'obligation {{ obligation.id }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col">
            <div class="card card-primary">
                <div class="card-header">
                    <div class="card-title">
                        Modiier l'obligation {{ obligation.id }}
                    </div>
                </div>
                <div class="card-body">
                    {{ include('apps/gestion_obligation/_form.html.twig', {'button_label': 'Modifier'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
