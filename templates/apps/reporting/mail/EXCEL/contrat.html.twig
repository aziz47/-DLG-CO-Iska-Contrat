{% extends 'apps/reporting/mail/EXCEL/base.html.twig' %}
{% block body %}
<table id="example1" class="table table-bordered table-hover">
    <thead>
    <tr>
        {% for h in headers %}
            <th>{{ h }}</th>
        {% endfor %}
    </tr>
    </thead>
    <tbody>
    {% for contrat in filtersData %}
    <tr>
        <td>{{contrat.id}}</td>
        <td>{{contrat.objet}}</td>
        <td>{{contrat.createdBy.displayName}}</td>
        <td>{{contrat.currentState | statutContrat}}</td>
        <td>{{contrat.createdAt | date('d/m/Y')}}</td>
        <td>{{contrat.typeDemande.lib}}</td>
    </tr>
    {% endfor %}
    </tbody>
    <tfoot>
    <tr>
        {% for h in headers %}
            <th>{{ h }}</th>
        {% endfor %}
    </tr>
    </tfoot>
</table>
{% endblock %}